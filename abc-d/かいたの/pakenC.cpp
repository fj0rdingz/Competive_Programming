#include <bits/stdc++.h>
#define ll long long
#define REP(i,m,n) for(ll i=(ll)(m);i<(ll)(n);i++)
#define rep(i,n) REP(i,0,n)
using namespace std;

int main() {
    cin.tie(0);
   	ios::sync_with_stdio(false);

    ll k,l,m,n,w,x,y,z,p,q,tmp=0;
    string str;
    cin>>n>>m;
    ll a[n+5][m+5];
    ll sum=0;
    rep(i,n)rep(j,m) cin>>a[i][j];
    rep(i,n)rep(j,m)rep(k,m){

        
}

    return 0;
}